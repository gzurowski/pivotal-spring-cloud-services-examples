spring:
  application:
    name: ${vcap.application.name:address-service}

security:
  basic:
    enabled: false
management:
  security:
    enabled: false

info:
  build:
    artifact: @project.artifactId@
    name: @project.name@
    description: @project.description@
    version: @project.version@
